function evenOrOdd(number) {
  if (!Number.isInteger(number)) {
    console.log("That's not a number!");
  } else if (num % 2 === 0) {
    console.log('even');
  } else {
    console.log('odd');
  }
}